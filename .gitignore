# Build output
/bin/
/out/

# Secrets — must never be committed (APNs key / FCM service account / env)
*.p8
*service-account*.json
*serviceAccount*.json
*firebase-adminsdk*.json
credentials.json
key.json
.env
.env.*

# Test / coverage artifacts
*.out
coverage.*
